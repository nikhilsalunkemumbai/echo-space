# Interfaces

The CLI interface source is not published here yet, for the same reason as the engine — see [engine/README.md](../engine/README.md).

**Phase 3 — Pygame visual layer: complete.** A mood-responsive visual interface with three panels, a pulsing mood ring, scrolling post feed, and crisis flash. Run with `pip install pygame numpy` once the engine is released.

**Coming in Phase 4:** A Godot-built downloadable binary.

Development updates are in [DEVLOG.md](../DEVLOG.md).
